<form method="post" action="{{ path('price_room_delete', {'id': price_room.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ price_room.id) }}">
    <button class="btn">Delete</button>
</form>
