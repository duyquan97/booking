{% extends 'base.html.twig' %}

{% block title %}PriceRoom{% endblock %}

{% block body %}
    <h1>PriceRoom</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ price_room.id }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ price_room.price }}</td>
            </tr>
            <tr>
                <th>Currency</th>
                <td>{{ price_room.currency }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('price_room_index') }}">back to list</a>

    <a href="{{ path('price_room_edit', {'id': price_room.id}) }}">edit</a>

    {{ include('price_room/_delete_form.html.twig') }}
{% endblock %}
